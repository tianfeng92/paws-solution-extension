{
  "name": "paws-solution-extension",
  "version": "1.0.0",
  "description": "A Chrome extension to analyze Semaphore CI failures using the Gemini API.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build:ts": "tsc --project tsconfig.json",
    "build:copy": "cp manifest.json dist; cp src/*.html dist; cp src/*.css dist",
    "build": "npm run clean && npm run build:ts && npm run build:copy"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "copyfiles": "^2.4.1",
    "prettier": "3.6.2",
    "typescript": "^5.4.5"
  }
}
